name: 'Build'

on:
  push:
    tags: [ '*.*.*' ]

concurrency:
  group: docker-${{ github.ref }}-${{ github.workflow }}
  cancel-in-progress: false

jobs:
  build:
    name: ${{ matrix.image }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - image: web
            context: "{{defaultContext}}:web"
          - image: proxy
            context: "{{defaultContext}}:proxy"
    uses: 'trexx/actions/.github/workflows/build.yml@70c2ee115f9d545af3d1d27fd0ee853aacddbba3' # main
    secrets: 'inherit'
    permissions:
      contents: read
      packages: write
    with:
      prune_images: false
      context: ${{ matrix.context }}
      images: ghcr.io/${{ github.repository }}-${{ matrix.image }}