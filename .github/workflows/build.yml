name: 'Build'

on:
  push:
    tags: [ '*.*.*' ]

concurrency:
  group: docker-${{ github.ref }}-${{ github.workflow }}
  cancel-in-progress: false

jobs:
  build:
    name: ${{ matrix.image }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - image: web
            context: "{{defaultContext}}:web"
          - image: proxy
            context: "{{defaultContext}}:proxy"
    uses: 'trexx/actions/.github/workflows/build.yml@8a041a75c5a97374c62e68894d2320e193c3377e' # main
    secrets: 'inherit'
    permissions:
      contents: read
      packages: write
    with:
      prune_images: false
      context: ${{ matrix.context }}
      images: ghcr.io/${{ github.repository }}-${{ matrix.image }}