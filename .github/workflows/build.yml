name: 'Build'

on:
  push:
    tags: [ '*.*.*' ]

concurrency:
  group: docker-${{ github.ref }}-${{ github.workflow }}
  cancel-in-progress: false

jobs:
  build:
    name: ${{ matrix.image }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - image: web
            context: web
          - image: proxy
            context: proxy
    uses: 'trexx/actions/.github/workflows/build.yml@8d0e044aa52d6e24ae7b75f585e1d9ddd59cd620' # main
    secrets: 'inherit'
    permissions:
      contents: read
      packages: write
    with:
      prune_images: false
      context: ${{ matrix.context }}
      image: ${{ matrix.image }}